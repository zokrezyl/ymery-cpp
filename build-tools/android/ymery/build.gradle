// Top-level build file
plugins {
    id 'com.android.application' version '8.2.0' apply false
}

// Repo root for build outputs
def repoRoot = rootProject.projectDir.parentFile.parentFile.parentFile
