import:
  - shared.editor

widgets:
  main-window:
    type: imgui.docking-main-window
    title: "Ymery Layout Editor"
    body:
      # Menu bar
      - imgui.main-menu-bar:
          body:
            - imgui.menu:
                label: "File"
                body:
                  - imgui.menu-item:
                      label: "New"
                  - imgui.separator
                  - imgui.menu-item:
                      label: "Export YAML..."
                  - imgui.separator
                  - imgui.menu-item:
                      label: "Exit"
            - imgui.menu:
                label: "Edit"
                body:
                  - imgui.menu-item:
                      label: "Clear All"
            - imgui.menu:
                label: "View"
                body:
                  - imgui.menu-item:
                      label: "Widget Browser"
                  - imgui.menu-item:
                      label: "Editor"
                  - imgui.menu-item:
                      label: "Code"
                  - imgui.menu-item:
                      label: "Preview"
                  - imgui.menu-item:
                      label: "Errors"
                  - imgui.menu-item:
                      label: "Logs"

      # Docking layout
      - imgui.docking-split:
          initial-dock: "MainDockSpace"
          new-dock: "BottomPanel"
          direction: "down"
          ratio: 0.20

      - imgui.docking-split:
          initial-dock: "MainDockSpace"
          new-dock: "TopPanel"
          direction: "up"
          ratio: 0.35

      - imgui.docking-split:
          initial-dock: "TopPanel"
          new-dock: "TopLeft"
          direction: "left"
          ratio: 0.25

      - imgui.docking-split:
          initial-dock: "TopPanel"
          new-dock: "TopRight"
          direction: "right"
          ratio: 0.40

      # Upper row
      - imgui.dockable-window:
          label: "Widget Browser"
          dock-space-name: "TopLeft"
          body:
            - editor.editor-widget-tree:

      - imgui.dockable-window:
          label: "Editor"
          dock-space-name: "TopPanel"
          body:
            - editor.editor:

      - imgui.dockable-window:
          label: "Code"
          dock-space-name: "TopRight"
          body:
            - editor.code-preview:

      # Middle - Preview
      - imgui.dockable-window:
          label: "Preview"
          dock-space-name: "MainDockSpace"
          body:
            - editor.preview:

      # Bottom - Errors and Logs
      - imgui.dockable-window:
          label: "Errors"
          dock-space-name: "BottomPanel"
          body:
            - debug.errors-view:

      - imgui.dockable-window:
          label: "Logs"
          dock-space-name: "BottomPanel"
          body:
            - debug.logs-view:

app:
  root-widget: main-window
