# ImGui Markdown plugin showcase
# Rich text rendering with markdown

widgets:
  demo:
    type: composite
    body:
      - imgui.text:
          label: "ImGui Markdown - Rich Text"
          style:
            imgui.text: [0.7, 0.9, 0.7, 1.0]
      - imgui.separator
      - imgui.text:
          label: "Markdown rendering in Dear ImGui"
      - imgui.separator

      - imgui.separator-text: "Headers"
      - imgui-markdown.markdown: |
          # Heading 1
          ## Heading 2
          ### Heading 3

      - imgui.separator-text: "Text Formatting"
      - imgui-markdown.markdown: |
          Regular text, **bold text**, and *italic text*.

          You can also use ~~strikethrough~~ and `inline code`.

      - imgui.separator-text: "Lists"
      - imgui-markdown.markdown: |
          Unordered list:
          * First item
          * Second item
          * Third item

          Ordered list:
          1. Step one
          2. Step two
          3. Step three

      - imgui.separator-text: "Links and Code"
      - imgui-markdown.markdown: |
          Visit [ImGui](https://github.com/ocornut/imgui) for more information.

          ```cpp
          void hello() {
              ImGui::Text("Hello, World!");
          }
          ```

      - imgui.separator-text: "Blockquotes"
      - imgui-markdown.markdown: |
          > This is a blockquote.
          > It can span multiple lines.

      - imgui.separator-text: "Horizontal Rule"
      - imgui-markdown.markdown: |
          Above the line

          ---

          Below the line
