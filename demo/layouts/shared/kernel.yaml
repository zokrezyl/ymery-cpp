# Kernel provider showcase
# Central hub for data providers

data:
  kernel:
    type: kernel

widgets:
  # Recursive kernel tree component
  kernel-recursive:
    type: composite
    foreach-child:
      body:
        - imgui.tree-node:
            label: "@label"
            openable: "@openable"
            opened: "@opened"
            style:
              openable:
                imgui.text: [0.5, 0.7, 1.0, 1.0]
              opened:
                imgui.text: [0.2, 1.0, 0.2, 1.0]
            body:
              - kernel-recursive:
            metadata-tooltip:
              body:
                - imgui.separator-text: "Metadata"
                - foreach-key:
                    body:
                      - imgui.text:
                          label: "@key: @value"

  demo:
    type: composite
    main-data: kernel
    body:
      - imgui.text:
          label: "Kernel Provider Hub"
          style:
            imgui.text: [0.7, 0.9, 0.7, 1.0]
      - imgui.separator
      - imgui.text:
          label: "Central routing to data providers"
      - imgui.separator

      - imgui.separator-text: "Providers"
      - imgui.text:
          label: "Click nodes to expand, hover for metadata"
      - imgui.separator

      - data-path: /providers
        kernel-recursive:

      - imgui.separator-text: "Settings"
      - data-path: /settings
        kernel-recursive:
