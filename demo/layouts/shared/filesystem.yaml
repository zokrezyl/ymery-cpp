# Filesystem browser showcase
# Tree-based file system navigation

data:
  filesystem:
    type: filesystem

widgets:
  # Recursive filesystem tree component
  fs-recursive:
    type: composite
    foreach-child:
      body:
        - tree-node:
            label: "@label"
            openable: "@openable"
            opened: "@opened"
            style:
              openable:
                text: [0.5, 0.7, 1.0, 1.0]
              opened:
                text: [0.2, 1.0, 0.2, 1.0]
            body:
              - filesystem.fs-recursive:
            metadata-tooltip:
              body:
                - separator-text: "Metadata"
                - foreach-key:
                    body:
                      - text:
                          label: "@key: @value"

  demo:
    type: composite
    main-data: filesystem
    body:
      - text:
          label: "Filesystem Browser"
          style:
            text: [0.7, 0.9, 0.7, 1.0]
      - separator
      - text:
          label: "Dynamic filesystem tree navigation"
      - separator

      - separator-text: "Available Locations"
      - text:
          label: "Click nodes to expand, hover for metadata"
      - separator

      - data-path: /available
        filesystem.fs-recursive:

      - separator-text: "Opened Files"
      - data-path: /opened
        filesystem.fs-recursive:
