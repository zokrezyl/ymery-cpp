* most of the widgets in the layout preview are not displaying what they should
* also the properties of different widgets should be extended and the metadata extended
* maybe instead of saving the metadata in yaml file as it is, an extra method/function should be exposed on the plugin that returns the "create" arguments description
